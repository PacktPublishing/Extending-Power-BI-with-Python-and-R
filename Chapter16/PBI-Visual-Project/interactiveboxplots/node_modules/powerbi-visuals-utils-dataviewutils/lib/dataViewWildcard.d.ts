export declare const enum DataViewWildcardMatchingOption {
    /** Match Identities and Totals (default) */
    InstancesAndTotals = 0,
    /** Match Instances with Identities only */
    InstancesOnly = 1,
    /** Match Totals only */
    TotalsOnly = 2
}
export declare function createDataViewWildcardSelector(dataViewWildcardMatchingOption?: DataViewWildcardMatchingOption): {
    data: {
        dataViewWildcard: {
            matchingOption: DataViewWildcardMatchingOption;
        };
    }[];
};
